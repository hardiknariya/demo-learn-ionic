import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'demo-learn-ionic',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
